<mvvmCrossViews:MvxWindow
    x:Class="FFmpeg.Gui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvmCrossViews="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:views="clr-namespace:FFmpeg.Gui.Views"
    Title="FFMpeg Gui"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TabControl TabStripPlacement="Bottom" x:Name="MainTabs">
            <TabItem Header="Source files">
                <views:FileSelectorView DataContext="{Binding FileSelectorVM}" />
            </TabItem>
            <TabItem Header="Preset">
                <views:PresetSelectorView DataContext="{Binding PresetSelectorVM}" />
            </TabItem>
        </TabControl>
        <StackPanel
            Grid.Row="1"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="PreviousButton"
                Content="Previous"
                Style="{StaticResource BottomButton}"
                Click="PreviousButton_Click"/>
            <Button
                x:Name="NextButton"
                Content="Next"
                Style="{StaticResource BottomButton}"
                Click="NextButton_Click"/>
        </StackPanel>
    </Grid>
</mvvmCrossViews:MvxWindow>
